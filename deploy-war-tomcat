pipeline {
agent any
stages {
stage ('deploy to tomcat')
{ steps {
sshagent(['tomcatdeployment']) {
sh 'scp -o StrictHostKeyChecking=no target/*.war ec2-user@172.31.93.186:/usr/share/tomcat/webapps
}
}
}
}
}
